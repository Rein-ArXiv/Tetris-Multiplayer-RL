"""Local-only Python netbot — joins a running C++ Tetris game over TCP.

This package mirrors the lockstep client path of the C++ ``net::Session`` so a
trained policy can play against a human (or another bot) without modifying
the C++ binary.
"""
