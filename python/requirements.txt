# pip fallback for lightweight local tests and rule-based netbot runtime.
#
# The canonical local workflow is now the root pyproject.toml:
#   uv sync --dev
#   uv run python -m pytest python/tests
#
# Keep this file for manual pip environments that should not pull PyTorch.
# The native `tetris_py` module is built separately via CMake (see python/sim/README.md).
numpy>=1.24
pytest>=7.4
